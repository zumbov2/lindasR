% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_neighbors.R
\name{get_neighbors}
\alias{get_neighbors}
\title{Retrieve neighboring triples for a LINDAS resource}
\usage{
get_neighbors(uri, direction = "both")
}
\arguments{
\item{uri}{Resource URI.}

\item{direction}{Direction of traversal. One of
\code{"out"}, \code{"in"}, or \code{"both"}.}
}
\value{
A tibble containing neighboring triples with columns:
  \code{s}, \code{p}, \code{o}, and \code{direction}.
}
\description{
\code{get_neighbors} retrieves triples connected to a given resource URI.
Both outgoing triples (where the URI is the subject) and incoming triples
(where the URI is the object) can be returned.
}
\examples{
\dontrun{
get_neighbors("https://ld.admin.ch/canton/21")

get_neighbors(
  "https://ld.admin.ch/canton/21",
  direction = "out"
)
}
}
